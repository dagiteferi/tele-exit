JWT_SECRET=change-me-to-a-real-secret
# Demo / production: keep false so Gemini, Tavily, YouTube, SMTP stay real.
USE_FAKES=false
DB_PATH=./tele_exit.db
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

GEMINI_API_KEY=
TAVILY_API_KEY=
YOUTUBE_API_KEY=
GOOGLE_CREDENTIALS_PATH=./credentials/google_service_account.json
# Calendar id (use "primary" when GOOGLE_DELEGATED_USER is set)
GOOGLE_CALENDAR_ID=primary
# Workspace user to impersonate for Gmail/Calendar API (optional; SMTP is easier for demos)
GOOGLE_DELEGATED_USER=

# --- Real demo email (required for Accept → calendar invite) ---
# Create a Gmail App Password: Google Account → Security → 2-Step Verification → App passwords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your.gmail@gmail.com
SMTP_PASSWORD=your-16-char-app-password
SMTP_FROM=your.gmail@gmail.com
SMTP_USE_TLS=true

LIVEKIT_URL=
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=

ADMIN_BOOTSTRAP_EMAIL=
ADMIN_BOOTSTRAP_PASSWORD=
FRONTEND_URL=http://localhost:3000
